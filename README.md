<div align="center">
PUBLIC KEY CRYPTOGRAPHY AND RSA
</div>

<p> RSA is the initials for Rivest, Aldma and Shamir. </p>

This contains everything you need to run your app locally.

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
